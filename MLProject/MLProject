name: BreastCancer_Classification

conda_env: conda.yaml

entry_points:
  main:
    parameters:
      data_dir: {type: string, default: "./BreastCancer_preprocessing"}
    command: "conda activate breast_cancer_env && python modelling.py --data-dir {data_dir}"
